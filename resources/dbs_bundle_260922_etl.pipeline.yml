# The main pipeline for dbs_bundle_260922

resources:
  pipelines:
    dbs_bundle_260922_etl:
      name: dbs_bundle_260922_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/dbs_bundle_260922_etl"

      libraries:
        - glob:
            include: ../src/dbs_bundle_260922_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
